EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_KEY=your-anon-key-here

# Google OAuth — create at https://console.cloud.google.com/apis/credentials
# Enable Google Sheets API + Google Drive API in your Google Cloud project.
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your-web-client-id.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=your-ios-client-id.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=your-android-client-id.apps.googleusercontent.com

# Supabase Edge Function secrets (Web client ID + secret only):
# supabase secrets set GOOGLE_CLIENT_ID=... GOOGLE_CLIENT_SECRET=...

# --- GOOGLE CLOUD SETUP (3 separate OAuth clients) ---
#
# 1) WEB client — Authorized redirect URIs (https/localhost ONLY, no custom schemes):
#      http://localhost:8081
#      http://localhost:19006
#    Authorized JavaScript origins:
#      http://localhost:8081
#    Do NOT add com.PairWise.app:/oauthredirect here — Google rejects non-https URIs.
#
# 2) ANDROID client — Type "Android" (NOT Web):
#      Package name: com.PairWise.app
#      SHA-1 fingerprint: from npm run android:sha1 (after npx expo run:android)
#    No redirect URI field on Android clients.
#
# 3) iOS client — Bundle ID: com.PairWise.app
#
# All three client IDs must be DIFFERENT values in .env above.
# --- EAS Android builds (required for Google Sign-In) ---
# EAS signs your APK with its own keystore — NOT your local debug key.
# 1) Run: eas credentials -p android
# 2) Copy the SHA-1 fingerprint
# 3) Google Cloud → Android OAuth client → Add fingerprint (keep package com.PairWise.app)
# 4) Optional: set env on EAS so app.config.js picks up client IDs at build time:
#    eas env:create --environment development --name EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID --value "..." --visibility plaintext
#
# OAuth consent screen: add your Gmail as a Test user while app is in Testing mode.
#
# --- EAS Update (over-the-air JS updates) ---
# Users on a release APK get JS/asset updates without reinstalling when you run:
#   npm run update:release -- --message "Describe your change"
# They must have an APK built AFTER OTA was enabled (channel: release).
# Bump app.json "version" and rebuild the APK when you change native code or add npm packages.
